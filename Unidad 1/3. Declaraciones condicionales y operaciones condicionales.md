#### `if`

![](imagenes/Pasted%20image%2020251102140500.png)
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("Give speed: ");
        int speed = scanner.nextInt();
        if (speed > 120){
            System.out.println("Speeding ticket!");
        }
    }
}
```
---
Es importante tener en cuenta la indentación al momento de usar el `if`: 
- *Ejemplo de un **mal** uso de indentación:* 
```java
if (number > 10) {
number = 9;
}
```

- *Ejemplo de un **buen** uso de indentación:*
```java
if (number > 10) {
    number = 9;
}
```
---
##### Operadores de Comparación
- `>` Mayor que
- `>=` Mayor o igual que
- `<` Menor que
- `<=` Menor que o igual que
- `==` Igual a 
- `!=` Diferente a

##### Ejercicios
![](imagenes/Pasted%20image%2020251102142443.png)
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("Give a number: ");
        int year = scanner.nextInt();
        if (year == 1984){
            System.out.println("Orwell");
        }
    }
}
```
---
#### `else`
Si la expresión dentro del paréntesis se evalúa como falsa, entonces el código se moverá al bloque de código siguiente fuera de la llave {}. Esto no siempre es deseado, por lo que generamos una opción alternativa cuando la condición expresada se evalúa como falsa.

```java
int number = 4;

if (number > 5) {
    System.out.println("Your number is greater than five!");
} else {
    System.out.println("Your number is five or less!");
}
```
##### Ejercicios 
![](imagenes/Pasted%20image%2020251102144139.png)
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner reader = new Scanner(System.in);
        System.out.println("Give a number: ");
        int number = reader.nextInt();
        if (number > 0){
            System.out.println("The number " + number + " is positive.");
        } else{
            System.out.println("The number " + number + " is negative.");
        }
    }
}
```
![](imagenes/Pasted%20image%2020251102144759.png)
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("How old are you?");
        int age = scanner.nextInt();
        if (age >= 18){
            System.out.println("You're " + age + ". So, you are an adult.");
        } else{
            System.out.println("You're " + age + ". So, you are not an adult.");
        }
        
    }
}
```
#### `else if`
```java
int number = 3;

if (number == 1) {
    System.out.println("The number is one");
} else if (number == 2) {
    System.out.println("The given number is two");
} else if (number == 3) {
    System.out.println("The number must be three!");
} else {
    System.out.println("Something else!");
}
```
![](imagenes/Pasted%20image%2020251102145459.png)
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner reader = new Scanner(System.in);
        System.out.println("Give the first number: ");
        int numberOne = reader.nextInt();
        System.out.println("Give the second number: ");
        int numberTwo = reader.nextInt();
        
        if (numberOne > numberTwo){
            System.out.println("Greater number is: " + numberOne);
        }else if (numberTwo > numberOne){
            System.out.println("Greater number is " + numberTwo);
        }else{
            System.out.println("The numbers are equal!");
        }
    }
}
```
---
#### Declaraciones condicionales y la variable Boolean

El valor que está dentro de los paréntesis de una declaración condicional debería ser de un tipo booleano después de la evaluación. Los tipos booleanos son o `true` o `false`
```java
boolean isItTrue = true;
System.out.println("The value of the boolean variable is " + isItTrue);
```
También se puede presentar de la siguiente forma:
```java
boolean isItTrue = true;
if (isItTrue) {
    System.out.println("Pretty wild!");
}
```
Los operadores de comparación también se pueden usar fuera del paréntesis de condicionalidad. En estos casos, el valor booleano resultado de una comparación se almacena en una variable booleana para usar después:
```java
int first = 1;
int second = 3;
boolean isGreater = first > second;
```
Por ejemplo, si hiciéramos fuera y almacenáramos dentro de una variable boolean el resultado:
```java
int first = 1;
int second = 3;
boolean isLessThan = first < second;

if (isLessThan) {
    System.out.println("1 is less than 3!");
}
```

##### Ejercicios 
![](imagenes/Pasted%20image%2020251102151957.png)
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("Give a number: ");
        int number = scanner.nextInt();
        if (number % 2 == 0){
            System.out.println("Number " + number + " is even");
        } else {
            System.out.println("Number " + number + " is odd");
        }
    }
}
```
#### Comparando Strings
```java
Scanner reader = new Scanner(System.in);

System.out.println("Enter the first string");
String first = reader.nextLine();
System.out.println("Enter the second string");
String second = reader.nextLine();

if (first == second) {
    System.out.println("The strings were the same!");
} else {
    System.out.println("The strings were different!");
}
```
Cuando comparamos strings también usamos el comando `equals` el cuál está relacionado a las variables string. Funciona de la siguiente manera: 
```java
Scanner reader = new Scanner(System.in);

System.out.println("Enter a string");
String input = reader.nextLine();

if (input.equals("a string")) {
    System.out.println("Great! You read the instructions correctly.");
} else {
    System.out.println("Missed the mark!");
}
```
##### Ejercicio
![](imagenes/Pasted%20image%2020251102170731.png)
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("Password?");
        String password = scanner.nextLine();
        if (password.equals("Caput Draconis")){
            System.out.println("Welcome!");
        }else{
            System.out.println("Off with you!");
        }
    }
}
```
---
#### Operadores lógicos

| Operadores | Significado |
| ---------- | ----------- |
| &&         | and         |
| \|\|       | or          |
| !          | not         |
*Ingresar un número entre un rango de 5 y 10*
```java
System.out.println("Is the number within the range 5-10: ");
int number = 7;

if (number >= 5 && number <= 10) {
    System.out.println("It is! :)");
} else {
    System.out.println("It is not :(")
}
```
Ingresar un número fuera del rango de 0 o 100
```java
System.out.println("Is the number less than 0 or greater than 100");
int number = 145;

if (number < 0 || number > 100) {
    System.out.println("It is! :)");
} else {
    System.out.println("It is not :(")
}
```
Si queremos invertir el resultado de una expresión `number > 4` using `!` por ejemplo usando el modificador el operador not.
```java
int number = 7;

if (!(number > 4)) {
    System.out.println("The number is not greater than 4.");
} else {
    System.out.println("The number is greater than 4.")
}
```
##### Ejercicio
![](imagenes/Pasted%20image%2020251102172350.png)
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner reader = new Scanner(System.in);
        
        System.out.println("How old are you?");
        int age = reader.nextInt();
        
        if (age >= 0 && age <= 120){
            System.out.println("OK");
        }else{
            System.out.println("Impossible!");
        }
    }
}
```
---
##### Orden de ejecución de las declaraciones condicionales
En el caso de esta declaración, si usáramos 15, deberíamos obtener *FizzBuzz*, sin embargo, obtenemos *buzz* porque se ejecuta primero y no ambos. Por lo que influye el orden.
```java
Scanner reader = new Scanner(System.in);

int number = Integer.valueOf(reader.nextLine());

if (number % 3 == 0) {
    System.out.println("Fizz");
} else if (number % 5 == 0) {
    System.out.println("Buzz");
} else if (number % 3 == 0 && number % 5 == 0) {
    System.out.println("FizzBuzz");
} else {
    System.out.println(number);
}
```
Para **resolver este problema**, hay que cambiar el orden de los condicionales: 
```java
Scanner reader = new Scanner(System.in);

int number = Integer.valueOf(reader.nextLine());

if (number % 3 == 0 && number % 5 == 0) {
    System.out.println("FizzBuzz");
} else if (number % 3 == 0) {
    System.out.println("Fizz");
} else if (number % 5 == 0) {
    System.out.println("Buzz");
} else {
    System.out.println(number);
}
```
##### Ejercicios:
![](imagenes/Pasted%20image%2020251102174012.png)
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner reader = new Scanner(System.in);
        System.out.println("Give a year: ");
        int year = reader.nextInt();
        
        if((year % 4 == 0 && year % 100 != 0) || year % 400 == 0){
            System.out.println("The year is leap year.");
        }else{
            System.out.println("The year is not a leap ");
        } 
    }
}
```
![](imagenes/Pasted%20image%2020251102180647.png)
![](imagenes/Pasted%20image%2020251102180731.png)
Versión como los ejercicios:
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner reader = new Scanner(System.in);
        System.out.println("Value of the gift? ");
        int gift = reader.nextInt();
        double tax = 0;
        if (gift < 5000){
            System.out.println("No tax!");
        }else if (gift >= 5000 && gift < 25000){
            tax = (100 + (gift - 5000)* 0.08);
            System.out.println("Tax: " + tax );
        }else if (gift >= 25000 && gift < 55000){
            tax = (1700 + (gift - 25000)* 0.10);
            System.out.println("Tax: " + tax );
        }else if (gift >= 55000 && gift < 200000){
            tax = (4700 + (gift - 55000)* 0.12);
            System.out.println("Tax: " + tax );
        }else if (gift >= 200000 && gift < 1000000){
            tax = (22100 + (gift - 200000)* 0.15);
            System.out.println("Tax: " + tax );
        }else{
            tax = (142100 + (gift - 1000000)* 0.17);
            System.out.println("Tax: " + tax );
        }
    }   
}
```
Versión simplificada:
```java
package practica;
import java.util.Scanner;
public class Practica {
    public static void main(String[] args) {
        Scanner reader = new Scanner(System.in);
        System.out.print("Value of the gift? ");
        int gift = reader.nextInt();
        double tax;

        if (gift < 5000) {
            System.out.println("No tax!");
        } else if (gift < 25000) {
            tax = 100 + (gift - 5000) * 0.08;
            System.out.println("Tax: " + tax);
        } else if (gift < 55000) {
            tax = 1700 + (gift - 25000) * 0.10;
            System.out.println("Tax: " + tax);
        } else if (gift < 200000) {
            tax = 4700 + (gift - 55000) * 0.12;
            System.out.println("Tax: " + tax);
        } else if (gift < 1000000) {
            tax = 22100 + (gift - 200000) * 0.15;
            System.out.println("Tax: " + tax);
        } else {
            tax = 142100 + (gift - 1000000) * 0.17;
            System.out.println("Tax: " + tax);
        }
    }
}

```